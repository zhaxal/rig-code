# Python dependencies for the greenhouse capture app.
#
# Install on Raspberry Pi OS 64-bit with:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# NOTE: ffmpeg is also required but is a SYSTEM package, not a pip package:
#   sudo apt install -y ffmpeg
# It is used to remux the encoded H.264/H.265 bitstream into a .mp4 container.

depthai>=3.0.0      # DepthAI v3 - OAK device API
opencv-python       # Live preview window + drawn touch UI + image saving
numpy               # Required by OpenCV / DepthAI frame handling
