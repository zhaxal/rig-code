# Python dependencies for the DepthAI v3 spatial detection example.
#
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# The model (yolov6-nano) is downloaded automatically from the Luxonis model
# zoo on first run, so no local model files are needed.

depthai>=3.0.0      # DepthAI v3 - OAK device API + SpatialDetectionNetwork
numpy               # depth-frame math
# opencv-python MUST be last: depthai pulls in opencv-python-headless,
# so installing opencv-python after it ensures the full (GUI) build wins.
opencv-python       # cv2.imshow display windows
